# Pillow Studio 技術規格文件

**專案定位：** 3D 客製化商品預覽工具（抱枕 + 馬克杯）  
**技術棧：** Astro + Three.js 0.170.0 + Blender GLB  
**最後更新：** 2026-06-07

---

## 📐 圖片編輯介面規格

### 設計原則
**紅框內的構圖 = 最終投影在 3D 物件上的構圖**

### 功能特性
- ✅ **自由縮放**：0.2 ~ 3.0 倍（可縮小留白或放大裁切）
- ✅ **自由拖曳**：隨時調整圖片位置
- ✅ **統一模式**：無模式切換，永遠可編輯
- ✅ **視覺提示**：紅框標示最終投影範圍，棋盤格代表透明/底色區域

### 使用情境
| 縮放範圍 | 效果 | 用途 |
|---------|------|------|
| 0.2 ~ 0.8 | 圖片縮小，周圍留白 | 完整圖案 + 顯露底色紋理 |
| 0.9 ~ 1.1 | 圖片剛好填滿 | 標準填充，無裁切 |
| 1.2 ~ 3.0 | 圖片放大，局部裁切 | 特寫細節，鋪滿表面 |

---

## 🛋️ 抱枕規格

### 狀態
✅ **完全正常** — 紅框內構圖與 3D 渲染完全一致

### 模型配置
- **模型檔案：** `public/pillow45.glb` (Blender 匯出)
- **尺寸：** 45×45 cm (1:1 方形)
- **材質：** 程序化布料紋理 (Canvas 生成 color/normal/roughness)
- **布料選項：** 棉布 / 亞麻 / 絲絨

### 圖片處理流程
```
用戶上傳圖片
    ↓
裁切編輯器（1:1 紅框）
    ↓
bakeCanvas('crop', transform, aspectRatio=1, flipVertical=false)
    ↓
Three.js CanvasTexture (flipY=true, 預設 WebGL 行為)
    ↓
套用到 Pillow Mesh
    ↓
✅ 渲染結果與紅框構圖一致
```

---

## ☕ 馬克杯規格

### 狀態
🔄 **重建中** — 已完成新模型分析與 UV 配置，待整合進前端

### 模型配置（新版 2026-06-07）
- **新模型：** `public/mug_new.glb` (來源: `/tmp/teamugblend.glb`, Blender 5.0)
- **頂點數：** 32,239 | **三角面：** 59,328
- **UV seam 展開：** Blender 手動標記 4 邊 seam → 矩形展開
- **Blender 原始檔：** `/tmp/teamugblend.blend` (Blender 5.0 格式)
- **Seam 分析數據：** `/tmp/seam_data.json`

### 座標系統
| | Blender (Z-up) | GLB (Y-up) |
|--|--|--|
| **上方** | +Z | +Y |
| **軸轉換** | X→X, Y→-Z, Z→Y | — |
| **手把方向** | atan2(y,x) ≈ -90° | atan2(z,x) ≈ +90° (+Z 方向) |
| **手把 seam 範圍** | -120.9° ~ -59.1° | +59.1° ~ +120.9° |

### UV 佈局
```
V=0.00 ┌──────────────────────────────┐
       │   杯口邊緣 (外+內重疊)        │
       │                              │
V≈0.06 │------ 可印刷區域上界 ---------|
       │                              │
       │   外側杯面 (可印刷)            │   ← shader 投影圖片的區域
       │   + 內側杯面 (重疊, 用法線排除) │
       │                              │
V≈0.45 │------ 可印刷區域下界 ---------|
       │                              │
V=0.50 ├──────────────────────────────┤
       │                              │
       │   杯底 / 杯緣                 │   ← 未來杯底圖案區域
       │                              │
V=0.99 └──────────────────────────────┘
       U=0.00                    U=1.00

       U=0.00 ← 手把中心 (-90° in GLB)
       U=0.40 ← 手把左邊界 seam
       U=0.50 ← 手把中心 (+90°)
       U=0.60 ← 手把右邊界 seam
       U=1.00 ← 手把中心 (= U=0.00, 繞一圈回來)
```

### UV 可印刷範圍（精確值）
| 參數 | 值 | 說明 |
|------|-----|------|
| **V_min** | 0.059 | 杯口安全邊距 |
| **V_max** | 0.447 | 杯底安全邊距 |
| **Handle U_min** | 0.40 | 手把左邊界 |
| **Handle U_max** | 0.60 | 手把右邊界 |
| **可印刷 U** | [0.00, 0.40] ∪ [0.60, 1.00] | 排除手把 |
| **可印刷 V** | [0.059, 0.447] | 排除杯口杯底 |

### 攤平長方形尺寸比（精確測量）
| 度量方式 | 寬 | 高 | 長寬比 |
|---------|-----|-----|--------|
| UV 空間 | U=0.800 | V=0.388 | 2.06:1 |
| 物理 3D (弧長×高) | 5.23 | 1.48 | **3.54:1** |
| **決定採用** | — | — | **3.5:1** |

> ⚠️ UV 展開不是等面積的：每單位 U ≈ 6.53 (3D), 每單位 V ≈ 3.81 (3D)。
> UV 尺度比 U/V = 1.72，shader 需做 aspect 補償。

### 使用者裁切框
- **主造型裁切比例：3.5:1** (精確物理比，避免圖像變形)
- **杯底裁切比例：1:1 圓形** (暫不變)

### Shader 過濾邏輯
```glsl
// 1. V 範圍過濾 (高度)
bool inVRange = vUv.y >= 0.059 && vUv.y <= 0.447;

// 2. U 範圍過濾 (排除手把)
bool inHandleU = vUv.x >= 0.40 && vUv.x <= 0.60;
bool isPrintable = inVRange && !inHandleU;

// 3. 法線過濾 (只顯示外側)
vec2 radialDir = normalize(vWorldPosition.xz);  // GLB 中 XZ 為水平面
vec2 normalDir = normalize(vWorldNormal.xz);
float outwardDot = dot(radialDir, normalDir);

if (isPrintable && outwardDot > 0.1) {
    // 重映射 UV 到 [0,1] 供 texture2D 取樣
    // 注意：U 有兩段，需合併成連續 [0,1]
    float remappedU;
    if (vUv.x < 0.40) {
        remappedU = vUv.x / 0.80;           // [0, 0.40] → [0, 0.5]
    } else {
        remappedU = (vUv.x - 0.60) / 0.80 + 0.5;  // [0.60, 1.00] → [0.5, 1.0]
    }
    float remappedV = (vUv.y - 0.059) / 0.388;  // [0.059, 0.447] → [0, 1]
    color = texture2D(map, vec2(remappedU, remappedV)).rgb;
}
```

### 兩個上傳區域

#### 1. 主造型（3.5:1 長方形）
- **裁切框比例：** 3.5:1 (寬:高) — 精確物理攤平比
- **預期位置：** 環繞杯身外側（排除手把區域）
- **bakeCanvas：** aspectRatio = 3.5

#### 2. 杯底（1:1 圓形）
- **裁切框形狀：** 1:1 方形 + 圓形遮罩
- **預期位置：** 杯底內部圓形區域
- **UV 範圍：** V ∈ [0.50, 0.99]（待實作極座標映射）

### 圖片處理流程（新版設計）
```
用戶上傳圖片
    ↓
裁切編輯器（3.5:1 紅框）  ← 精確物理比
    ↓
bakeCanvas('crop', transform, aspectRatio=3.5, flipVertical=false)
    ↓
Three.js CanvasTexture (flipY=false)
    ↓
Shader UV 過濾:
  - V ∈ [0.059, 0.447] (高度)
  - U ∉ [0.40, 0.60] (排除手把)
  - outwardDot > 0.1 (只顯示外側)
    ↓
UV 重映射: U兩段合併 → [0,1], V標準化 → [0,1]
    ↓
✅ 渲染結果與紅框構圖一致（3.5:1 = 物理攤平比）
```

### 已知問題清單

#### ~~1. 主造型 UV 映射錯誤~~ ✅ 已解決
- **原因：** 舊模型 UV 可印刷區域長寬比 1.09:1，紅框 3:1，差異 2.75 倍
- **解決：** 新 Blender seam 展開 + 精確測量物理比 3.54:1 → 裁切框改 3.5:1

#### 2. 杯底扭曲變形 ⚠️ 待修正
- **現象：** 方形 UV 直接映射到圓形杯底，缺少極座標轉換
- **方向：** 使用 V ∈ [0.50, 0.99] 區域 + 極座標映射

#### ~~3. 手把區域白條~~ ✅ 已解決
- **解決：** 新 shader 用 `U ∉ [0.40, 0.60]` 排除手把，無白條問題

---

## 🛠️ 待修正項目

### 優先級 P0（核心功能）
- [x] ~~修正主造型 UV 映射~~ → 新模型 + 3.5:1 裁切框
- [x] ~~確保手把區域不顯示圖案~~ → shader U 排除 [0.40, 0.60]
- [ ] 將新 GLB 複製到 `public/mug_new.glb` 並更新前端載入路徑
- [ ] 前端裁切框 aspectRatio 從 3 改為 3.5
- [ ] Shader 整合新 UV 重映射邏輯（U 兩段合併）
- [ ] 修正杯底圓形 UV 映射（極座標轉換）

### 優先級 P1（體驗優化）
- [ ] 移除舊的 `mug_uv_config.json`（5.6MB，改用 shader 內建常數）
- [ ] 驗證所有縮放/拖曳情境下的一致性
- [ ] 新增馬克杯預覽的多角度視圖（正面/側面/俯視）

---

## 📝 開發筆記

### 抱枕成功要素
1. ✅ 使用標準 `flipY=true` (WebGL 預設行為)
2. ✅ UV 映射簡單直接（平面投影）
3. ✅ 無複雜幾何（無手把、無內外側區分）

### 馬克杯挑戰點（新版解決方案）
1. ✅ 圓柱體 UV 展開 → Blender 手動 seam 標記 + unwrap
2. ✅ 裁切框比例 → 精確物理攤平比 3.5:1
3. ✅ 手把區域過濾 → shader 排除 U ∈ [0.40, 0.60]
4. ⚠️ 杯底圓形 → 極座標投影（待實作）
5. ⚠️ UV 非等面積展開 → U/V 尺度比 1.72，裁切框已用物理比補償

### Blender ↔ GLB 座標轉換備忘
```
Blender Z-up → GLB Y-up
  X → X (不變)
  Y → -Z (前方變右方)  
  Z → Y (上方不變)
  
Blender atan2(y,x) → GLB atan2(z,x) 角度差 180°
手把: Blender -90° → GLB +90°
```

---

## 🔗 相關檔案

### 前端
- `src/pages/pillow-studio.astro` — HTML 結構 + 樣式
- `public/pillow-studio.js` — Three.js 邏輯 + 圖片編輯

### 3D 模型
- `public/pillow45.glb` — 抱枕模型
- `public/mug_new.glb` — 馬克杯新模型 (待部署, 來源: `/tmp/teamugblend.glb`)
- ~~`public/mug_complete.glb`~~ — 舊版模型 (18,556 面, 待移除)
- ~~`public/mug_uv_config.json`~~ — 舊版 UV 配置 (5.6MB, 待移除)

### 分析數據
- `/tmp/seam_data.json` — Blender seam edge 完整數據
- `/tmp/export_seams.py` — Blender 5.x seam 匯出腳本
- `/tmp/mug_test/index.html` — 紅色投影測試頁面

---

**文件版本：** v2.0  
**建立日期：** 2025-06-06  
**最後更新：** 2026-06-07  
**維護者：** Justin Huang
